`use strict`;

// creating a date object

// const now = new Date();

// const hours = now.getHours();

// const firstName = prompt("Enter your name: ");

// if (hours > 12) {
//   alert(`Good Aftrnoon: ${firstName}`);
//   console.log(
//     `Time: ${now.getHours()}:${now.getMinutes()}:${now.getSeconds()}`
//   );
// } else if (hours < 12) {
//   alert(`Good Morning: ${firstName}`);
// } else {
//   console.log(`Good Night: ${firstName}`);
// }

// const age = Number(prompt(`Enter your Age: `));

// const discount =
//   age >= 65 || age <= 6 ? `Eligible for discount` : `Sorry, No discount`;

// console.log(discount);

// Checking condition with the holiday

// let isHoliday = false;
// let age = Number(prompt(`Enter your age: `));

// const discount =
//   (age >= 65 || age <= 6) && !isHoliday
//     ? "Congrats, you are eligible for holiday"
//     : `Sorry, you are not eligible for holiday`;

// console.log(discount);
